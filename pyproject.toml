[project]
name = "youtube-local-exporter-native-host"
version = "0.1.0"
description = "Native messaging host for YouTube Local Exporter"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["native-host/tests"]
